#!/usr/bin/env bash
set -eou pipefail

cd $CG_PROJECT_ROOT
cd backend
eval "$(./shell.py $CG_STAGE --dry-run)"
backend.bootstrap

cd $CG_PROJECT_ROOT
cd appgen
eval "$(./shell.py $CG_STAGE --dry-run)"
appgen.bootstrap
