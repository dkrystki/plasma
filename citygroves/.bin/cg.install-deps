#!/usr/bin/env bash
set -eou pipefail

pl.install-kind "$CG_KIND_VER" "$CG_BIN_PATH"
pl.install-kubectl "$CG_KUBECTL_VER" "$CG_BIN_PATH"
pl.install-skaffold "$CG_SKAFFOLD_VER" "$CG_BIN_PATH"
pl.install-helm "$CG_HELM_VER" "$CG_BIN_PATH"
pl.install-hostess "$CG_BIN_PATH"
