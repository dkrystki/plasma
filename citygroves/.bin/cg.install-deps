#!/usr/bin/env bash
set -eou pipefail

# $1 = where to install

pl.install-kind "$CG_KIND_VER" "$1"
pl.install-kubectl "$CG_KUBECTL_VER" "$1"
pl.install-skaffold "$CG_SKAFFOLD_VER" "$1"
pl.install-helm "$CG_HELM_VER" "$1"
pl.install-hostess "$1"
