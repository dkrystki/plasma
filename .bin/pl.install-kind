#!/usr/bin/env bash
set -eou pipefail

# $1 = version
# $2 = destination folder

echo "Installing kind"
cd /tmp
curl -Lso kind "https://github.com/kubernetes-sigs/kind/releases/download/v$1/kind-$(uname)-amd64"
chmod +x kind
mv kind "$2/kind"
