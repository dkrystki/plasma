#!/usr/bin/env bash
set -eou pipefail

# $1 = version
# $2 = destination folder

echo "Installing kubectl"
cd /tmp
curl -Lso kubectl "https://storage.googleapis.com/kubernetes-release/release/v$1/bin/linux/amd64/kubectl"
chmod +x kubectl
mv kubectl $2/kubectl
